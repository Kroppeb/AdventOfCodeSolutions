@file:Suppress("PackageDirectoryMismatch", "UnusedImport")

package solutions.y21.d24

/*
import grid.Clock
import helpers.*
import itertools.*
import kotlin.math.*
 */

import grid.Clock
import helpers.*

private val xxxxx = Clock(6, 3);

/*
    So I did day 24 mostly by "decompiling" the alu code, and I didn't save my code anywhere
    I do have a recording of it, but I'm not in the mood to transcribe it
*/

fun main() {
    println("Day 24: ")
}

private fun <T> T.log(): T = also { println(this) }